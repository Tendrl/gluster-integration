============
Installation
============

At the command line::

    $ pip install gluster_bridge

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gluster_bridge
    $ pip install gluster_bridge
